name: My first workflow

on:
  - push

jobs:
  say-hello:
    name: Say Hello
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Say Hello
        run: echo "Hello"
